#include "core/Batch.h"
#include "internal/core.default_shaders.internal.h"

batch load_batch(shader_program quad_shader)
{
	batch buffer;
	buffer.quad_shader_program = quad_shader;
	buffer.
}